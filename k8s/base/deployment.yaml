apiVersion: apps/v1
kind: Deployment
metadata:
  name: scron
spec:
  replicas: 0
  selector:
    matchLabels:
      app: scron
  template:
    metadata:
      labels:
        app: scron
    spec:
      containers:
        - name: app
          image: ghcr.io/krishnarajt/scron:git-de650ccf87c4b0cc2e68f3f9dd3b4ac8e001f24f
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
              name: http

          envFrom:
            - secretRef:
                name: scron-env

          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10

          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 5

          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
